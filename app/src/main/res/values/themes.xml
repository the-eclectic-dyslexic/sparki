<resources>
    <!-- Base application theme. -->
    <style name="Theme.Sparki" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimaryDark">@color/dark_purple</item>
        <item name="colorPrimary">@color/dark_purple</item>
        <item name="colorAccent">@color/light_purple</item>
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="colorTertiary">@color/slight_purple</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:popupBackground">?attr/colorOnPrimary</item>
    </style>

    <style name="Theme.Sparki.AppBarOverlay" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="android:textColorPrimary">?attr/colorOnPrimary</item>
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="Theme.Sparki.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">

    </style>

    <style name="Theme.Sparki.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Sparki.Switch">
        <item name="android:textSize">16sp</item>
        <item name="colorControlActivated">?attr/colorPrimary</item>
        <item name="android:paddingTop">@dimen/normal_padding</item>
        <item name="android:paddingBottom">@dimen/normal_padding</item>
        <item name="android:paddingStart">@dimen/normal_padding</item>
        <item name="android:paddingEnd">0dp</item>
    </style>

    <style name="Theme.Sparki.NumberPicker">
        <item name="android:textColorPrimary">?attr/colorPrimary</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Sparki.TextInput" parent="ThemeOverlay.Material3.TextInputEditText">
        <item name="backgroundTint">?attr/colorTertiary</item>
        <item name="android:textColorHint">?attr/colorAccent</item>
        <item name="android:inputType">text</item>
    </style>

</resources>